# SUTRA Core - Python Dependencies
#
# Install the package with pip to get all dependencies:
#   pip install -e .              # minimal install (just the app)
#   pip install -e ".[dev]"       # with dev tools (lint, format)
#   pip install -e ".[test]"      # with test libraries
#   pip install -e ".[prod]"      # with production extras
#   pip install -e ".[all]"       # everything
#
# This file is kept for compatibility with older workflows.
# The canonical dependency list is in pyproject.toml.

-e .
